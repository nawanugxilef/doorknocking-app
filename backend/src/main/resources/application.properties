spring.application.name=backend

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/doorknocking}
spring.datasource.username=${DATABASE_USERNAME:pepelgunawan}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
server.port=${PORT:8080}

app.jwt.secret=${JWT_SECRET:Z29vZC1kZWZhdWx0LWRvb3Jrbm9jay1qd3Qtc2VjcmV0LWZvci1sb2NhbC1kZXY=}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,https://*.vercel.app}

app.bootstrap.admin.enabled=${BOOTSTRAP_ADMIN_ENABLED:true}
app.bootstrap.admin.full-name=${BOOTSTRAP_ADMIN_FULL_NAME:Anthony Admin}
app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:admin@doorknock.test}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:password123}

app.auth.verification-link-expiry-minutes=${AUTH_VERIFICATION_LINK_EXPIRY_MINUTES:1440}
app.auth.verification-resend-cooldown-seconds=${AUTH_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
app.email.provider=${APP_EMAIL_PROVIDER:brevo}
app.email.from=${APP_MAIL_FROM:no-reply@doorknock.local}
app.email.from-name=${APP_MAIL_FROM_NAME:Doorknock}
app.email.frontend-url=${APP_FRONTEND_URL:http://localhost:3000}
app.email.brevo-api-key=${BREVO_API_KEY:}
app.email.webhook-url=${APP_EMAIL_WEBHOOK_URL:}
app.email.webhook-secret=${APP_EMAIL_WEBHOOK_SECRET:}
